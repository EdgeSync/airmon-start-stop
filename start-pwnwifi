#Copy this to /usr/local/bin
#add sudo to start of commands if you are a non-privelaged user (not needed if root)
#ensure you set chmod +x for your user account on the file

ifconfig wlan0 down
airmon-ng check kill
airmon-ng start wlan1
